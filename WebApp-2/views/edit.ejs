<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profiles</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="h-screen w-full bg-zinc-900 flex text-white">
      <div class="w-full h-[400px] p-5">
        <h1
          class="text-3xl text-white/40 flex mt-20 justify-center w-full items-center cursor-not-allowed"
        >
          UPDATE USER
        </h1>
        <div class="flex flex-col mt-8 items-center h-full">
          <form action="/update/<%= user._id %>" method="post">
            <div class="flex flex-col justify-center items-center">
              <div>
                <input
                  class="px-5 py-3 w-full bg-transparent border-[1px] border-zinc-700 rounded-md"
                  name="name"
                  placeholder="Name"
                  value="<%= user.name %>"
                  required
                />
                <input
                  class="px-5 py-3 w-full mt-5 bg-transparent border-[1px] border-zinc-700 rounded-md"
                  name="email"
                  type="email"
                  placeholder="Email"
                  value="<%= user.email %>"
                  required
                />
                <input
                  class="px-5 py-3 w-full mt-5 bg-transparent border-[1px] border-zinc-700 rounded-md"
                  name="image"
                  placeholder="Image url"
                  value="<%= user.image %>"
                  required
                />
              </div>
              <input
                class="px-5 py-3 rounded-md bg-yellow-500 mt-10 w-[70%] text-lg text-black cursor-pointer hover:bg-yellow-600"
                type="submit"
                value="Update"
              />
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
